# Throwaway data dirs for `just client <name>` isolated test instances.
/tmp/

# Build output for npm publishing.
/dist/

# Local prod-like server data (compose.yaml mounts ./data into the container)
/server/data/
/server/.env
